# SQLite en local; en Railway se monta un volumen en /data.
# postgres:// se normaliza solo a postgresql:// si se pasa a Postgres.
DATABASE_URL=sqlite:///./parte_del_dia.db

# Zona horaria para fechas de apuntes y avisos.
TZ_LOCAL=Europe/Madrid

# Token del bot de Telegram, el que da @BotFather.
TELEGRAM_BOT_TOKEN=

# Hora local del resumen automático de cada noche.
HORA_AVISO=21:00

# Webs que pueden llamar a la API, separadas por comas.
# En local, el servidor de Vite; en Railway, la URL del front.
CORS_ORIGENES=http://localhost:5173
